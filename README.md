# project Development steps
## project Development steps
### project Development steps
#### project Development steps
1.create floder
2.create virtual Environment
3.Activate virtual Environment
4.Add pre-requirements to requirments.txt
5.Install Pre-reuirements
6.Add virtual environment name o .gitignorefile
7.Implement library system
**step 1:**
1. create folder command: <br>
```mkdir library```

**step 2:**
2. create virtual environment: <br>
```python -m venv library_env```

**step 3:**Activate virtual environment <br>
```library_env\scripts\activate```

- create ```requirments.txt```file
**step 4:**add pre-requirements to reuirements.txt

**step 5:**install pre-requirement <br>
```pip install -r requirements.txt```
- create```.gitignore```file

**step 6:**add virtual environment name to .gitignore file

**step 7:** implement library management system